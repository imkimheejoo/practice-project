language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew wrapper --gradle-version 5.6"

#CI 실행 완료시 메일로 알림
notifications:
  email:
    recipients:
      - ddkkmd@naver.com